# Video-Centric Data Curation Pipeline
# Install with: pip install -r requirements.txt
#
# Target Model: OpenGVLab/InternVL3_5-8B (8.5B parameters)
# https://huggingface.co/OpenGVLab/InternVL3_5-8B

# Core dependencies
numpy>=1.24.0
torch>=2.0.0
torchvision>=0.15.0        # For image transforms
transformers>=4.45.0       # InternVL requires recent version

# Video/Image processing
decord>=0.6.0              # Preferred for video performance
av>=10.0.0                 # Fallback option (PyAV)
Pillow>=9.0.0              # For frame directory loading (JPG files)

# InternVL dependencies
einops>=0.6.0              # Tensor operations
timm>=0.9.0                # Vision models

# Graph & Clustering
networkx>=3.0
scikit-learn>=1.3.0
scipy>=1.10.0

# Leiden algorithm (primary - recommended)
python-igraph>=0.10.0
leidenalg>=0.10.0

# Alternative clustering (fallback if leidenalg not available)
# cdlib>=0.3.0

# Approximate nearest neighbors (optional, for large datasets)
pynndescent>=0.5.0

# Progress bar
tqdm>=4.65.0

# =============================================================================
# Visualization dependencies
# =============================================================================

# Interactive charts
plotly>=5.0.0              # Sunburst, Treemap, interactive scatter

# Dimensionality reduction
umap-learn>=0.5.0          # UMAP for embedding visualization

# Network visualization
pyvis>=0.3.0               # Interactive network graphs

# Static plotting
matplotlib>=3.7.0          # Static charts and figures

# Data manipulation (for distribution charts)
pandas>=2.0.0

# Dashboard (optional - for interactive exploration)
# streamlit>=1.20.0        # Run: pip install streamlit

# =============================================================================
# Optional dependencies
# =============================================================================

# Optional: Flash Attention for faster inference (requires CUDA)
# pip install flash-attn --no-build-isolation
# flash-attn>=2.0.0

# Optional: For external VLM captioning APIs
# openai>=1.0.0             # For GPT-4V integration
# anthropic>=0.20.0         # For Claude Vision integration
