# Diversity Metrics for Autonomous Driving Data Evaluation

## 개요

이 문서는 자율주행 데이터셋의 다양성을 정량적으로 평가하기 위한 지표들의 이론적 배경, 계산 방법, 그리고 해석 지침을 설명합니다.

자율주행 시스템의 안전성과 robustness를 보장하기 위해서는 학습 데이터가 다양한 시나리오를 충분히 커버해야 합니다. 그러나 "다양성"은 본질적으로 ground truth가 없는 개념이므로, 본 모듈에서는 다음 두 가지 접근법을 사용합니다:

1. **절대적 다양성 지표**: 데이터셋 자체의 분포 특성 측정
2. **상대적 다양성 지표**: 참조 데이터셋(BDD100K, nuImages) 대비 커버리지 측정

---

## 구현된 지표

### 1. Vendi Score (벤디 스코어)

#### 이론적 배경

Vendi Score는 데이터셋의 "실효 다양성(effective diversity)"를 측정하는 지표로, 유사도 행렬의 고유값에 대한 Shannon 엔트로피의 지수로 정의됩니다.

**수식:**
```
VS = exp(-Σ λᵢ log(λᵢ))
```

여기서 λᵢ는 정규화된 유사도 행렬 K의 고유값입니다.

#### 직관적 해석

- **의미**: "실질적으로 구별 가능한 고유 시나리오의 수"
- **범위**: [1, n_samples]
- **예시**: VS = 50이면 데이터셋에 약 50개의 실질적으로 구별되는 시나리오가 존재
- **백분율 변환**: (VS / n_samples) × 100%

#### 참고 논문

> **Friedman, D., & Dieng, A. B. (2023)**
> "The Vendi Score: A Diversity Evaluation Metric for Machine Learning"
> *International Conference on Learning Representations (ICLR)*
> https://arxiv.org/abs/2210.02410

#### 해석 가이드

| 점수 범위 (%) | 해석 |
|--------------|------|
| > 50% | 우수: 대부분의 샘플이 고유함 |
| 30-50% | 양호: 적절한 수준의 다양성 |
| 10-30% | 보통: 일부 중복/유사 샘플 존재 |
| < 10% | 낮음: 많은 샘플이 유사함, 데이터 수집 확대 필요 |

---

### 2. Coverage Score (커버리지 스코어)

#### 이론적 배경

Coverage는 참조 데이터셋의 manifold를 평가 데이터셋이 얼마나 "커버"하는지 측정합니다. k-NN 기반 방법으로, 참조 데이터의 각 샘플에 대해 평가 데이터에서 충분히 가까운 샘플이 있는지 확인합니다.

**수식:**
```
Coverage = (1/n_ref) × Σᵢ 𝟙[min_j d(xᵢʳᵉᶠ, xⱼᵉᵛᵃˡ) ≤ τᵢ]
```

여기서 τᵢ는 참조 데이터셋 내 k-NN 거리에서 결정된 임계값입니다.

#### 직관적 해석

- **의미**: "참조 데이터셋의 시나리오 중 몇 %를 커버하는가"
- **범위**: [0%, 100%]
- **예시**: BDD100K 대비 Coverage = 70%면 BDD의 70% 시나리오를 커버

#### 참고 논문

> **Kynkäänniemi, T., Karras, T., Laine, S., Lehtinen, J., & Aila, T. (2019)**
> "Improved Precision and Recall Metric for Assessing Generative Models"
> *Neural Information Processing Systems (NeurIPS)*
> https://arxiv.org/abs/1904.06991

#### 해석 가이드

| 점수 범위 (%) | vs BDD100K 해석 |
|--------------|----------------|
| > 70% | 우수: BDD의 대부분 시나리오 커버 |
| 50-70% | 양호: 주요 시나리오 대부분 포함 |
| 30-50% | 보통: 핵심 시나리오 일부만 커버 |
| < 30% | 제한적: BDD 대비 시나리오 부족 |

---

### 3. Density Score (밀도 스코어)

#### 이론적 배경

Density는 평가 데이터셋의 샘플들이 참조 데이터셋의 고밀도 영역에 얼마나 위치하는지 측정합니다. k-NN 기반 커널 밀도 추정을 사용합니다.

**수식:**
```
Density = (1/kn) × Σᵢ Σⱼ 𝟙[d(xᵢᵉᵛᵃˡ, xⱼʳᵉᶠ) ≤ rⱼ]
```

여기서 rⱼ는 참조 데이터셋에서 j번째 샘플의 k-NN 반경입니다.

#### 직관적 해석

- **의미**: "평가 데이터가 참조 분포의 핵심 영역에 있는 정도"
- **범위**: [0%, ∞) (일반적으로 100% 이하)
- **예시**: Density = 80%면 샘플들이 참조 분포의 핵심 영역에 밀집

#### 참고 논문

> **Naeem, M. F., Oh, S. J., Uh, Y., Choi, Y., & Yoo, J. (2020)**
> "Reliable Fidelity and Diversity Metrics for Generative Models"
> *International Conference on Machine Learning (ICML)*
> https://arxiv.org/abs/2002.09797

---

### 4. Feature Space Coverage (특성 공간 커버리지)

#### 이론적 배경

임베딩 공간을 그리드로 분할하여 점유된 셀의 비율을 측정합니다. PCA로 차원 축소 후 각 차원을 n_bins로 양자화합니다.

**수식:**
```
FSC = |occupied_cells| / |total_cells| × 100%
```

상대적 FSC (참조 데이터셋 대비):
```
Relative_FSC = |occupied ∩ reference_occupied| / |reference_occupied| × 100%
```

#### 직관적 해석

- **절대적 FSC**: "특성 공간의 몇 %를 점유하는가"
- **상대적 FSC**: "참조 데이터가 점유한 공간 중 몇 %를 커버하는가"

#### 참고 논문

> **Xu, Y., et al. (2022)**
> "On the Diversity of Autonomous Driving Datasets"
> *CVPR 2022 Workshop on Autonomous Driving*

---

### 5. Scenario Entropy (시나리오 엔트로피)

#### 이론적 배경

클러스터(시나리오) 분포의 Shannon 엔트로피를 측정합니다. 균등 분포일 때 최대 엔트로피를 가집니다.

**수식:**
```
H = -Σᵢ pᵢ log(pᵢ)
Normalized_H = H / log(n_clusters) × 100%
```

여기서 pᵢ는 클러스터 i의 샘플 비율입니다.

#### 직관적 해석

- **의미**: "시나리오가 얼마나 균등하게 분포되어 있는가"
- **범위**: [0%, 100%]
- **100%**: 완전 균등 분포 (모든 클러스터 크기 동일)
- **낮은 값**: 특정 시나리오에 편중

#### 참고 자료

> **Shannon, C. E. (1948)**
> "A Mathematical Theory of Communication"
> *Bell System Technical Journal*

---

### 6. Balance Score (균형 스코어)

#### 이론적 배경

Gini 계수의 보수(1 - Gini)로, 클러스터 크기 분포의 불평등도를 측정합니다.

**수식:**
```
Gini = Σᵢ Σⱼ |sᵢ - sⱼ| / (2n Σₖ sₖ)
Balance = (1 - Gini) × 100%
```

여기서 sᵢ는 클러스터 i의 크기입니다.

#### 직관적 해석

- **의미**: "클러스터 크기가 얼마나 균등한가"
- **범위**: [0%, 100%]
- **100%**: 완전 균등
- **낮은 값**: Long-tail 분포 (소수 클러스터가 대부분 샘플 보유)

---

### 7. Rarity Score (희귀도 스코어)

#### 이론적 배경

각 샘플의 k-NN 평균 거리를 계산하여 "희귀"한 (즉, 다른 샘플과 멀리 떨어진) 샘플을 식별합니다.

**수식:**
```
Rarity(xᵢ) = mean(d(xᵢ, nn_k(xᵢ)))
Rare_Ratio = |{i : Rarity(xᵢ) > threshold}| / n × 100%
```

#### 직관적 해석

- **의미**: "희귀/엣지 케이스 시나리오 비율"
- **최적 범위**: 5-15%
- **너무 낮음 (< 5%)**: 엣지 케이스 부족, 안전성 테스트 제한적
- **너무 높음 (> 20%)**: 데이터 품질 확인 필요

---

### 8. Intra-Cluster Diversity (클러스터 내 다양성)

#### 이론적 배경

각 클러스터 내 샘플들 간의 평균 거리를 측정합니다. 클러스터링이 잘 되었다면 낮은 값이 나와야 합니다.

**수식:**
```
ICD(cluster_k) = mean(d(xᵢ, xⱼ)) for xᵢ, xⱼ ∈ cluster_k
Mean_ICD = weighted_mean(ICD(cluster_k)) × 100%
```

#### 직관적 해석

- **의미**: "같은 시나리오 내 샘플들의 변이 정도"
- **높은 값**: 클러스터가 이질적 → 더 세분화된 클러스터링 필요
- **낮은 값**: 클러스터가 동질적 → 시나리오 구분이 잘 됨

---

## 종합 다양성 스코어 (Overall Diversity Score)

### 계산 방법

개별 지표들의 가중 평균으로 계산합니다. 가중치는 자율주행 맥락에서의 중요도를 반영합니다:

```python
weights = {
    "vendi":            0.20,  # 실효 다양성
    "entropy":          0.15,  # 분포 균등성
    "balance":          0.15,  # 클러스터 균형
    "coverage":         0.25,  # 절대 커버리지 (가장 중요)
    "relative_coverage": 0.15,  # 참조 대비 커버리지
    "rarity":           0.10,  # 희귀 시나리오 (조정됨)
}
```

### Rarity 점수 조정

희귀도는 최적 범위(5-15%)에서 가장 높은 점수를 받도록 조정됩니다:
- 0% → 50점
- 5-15% → 100점
- 30%+ → 50점

---

## 참조 데이터셋

### BDD100K

- **설명**: Berkeley Deep Drive에서 제공하는 100K 프레임 규모의 주행 영상 데이터셋
- **특징**: 다양한 날씨, 시간대, 지역 포함
- **URL**: https://www.bdd100k.com/

### nuImages

- **설명**: nuScenes 프로젝트의 이미지 데이터셋
- **특징**: Boston, Singapore 등 다양한 도시 환경
- **URL**: https://www.nuscenes.org/nuimages

---

## 사용 예시

```python
from diversity_metrics import DiversityEvaluator
from video_curation_pipeline import VideoCurationPipeline

# 파이프라인 실행
pipeline = VideoCurationPipeline()
results = pipeline.run_from_embeddings(embeddings, video_names)

# 다양성 평가
evaluator = DiversityEvaluator(embedder=pipeline.embedder)
report = evaluator.evaluate(
    results,
    dataset_name="my_dataset",
    reference_datasets=["bdd100k", "nuimages"],
    bdd_path="/path/to/bdd100k",
    nuimages_path="/path/to/nuimages"
)

# 결과 출력
from diversity_metrics.report import print_report_summary, generate_html_report
print_report_summary(report)
generate_html_report(report, "diversity_report.html")
```

---

## 지표 선택 가이드

| 목적 | 권장 지표 |
|------|----------|
| 전체 다양성 평가 | Overall Diversity Score |
| 시나리오 커버리지 | Coverage Score, FSC |
| 분포 균형 확인 | Scenario Entropy, Balance Score |
| 엣지 케이스 탐지 | Rarity Score |
| 클러스터링 품질 | Intra-Cluster Diversity |
| 절대적 다양성 | Vendi Score |

---

## 한계점 및 주의사항

1. **임베딩 품질 의존성**: 모든 지표가 임베딩 품질에 의존합니다. 부적절한 모델 사용 시 결과가 왜곡될 수 있습니다.

2. **참조 데이터셋 편향**: BDD100K, nuImages도 특정 지역/조건에 편향되어 있으므로, 상대적 지표 해석 시 주의가 필요합니다.

3. **시나리오 정의의 모호성**: "시나리오"의 정의는 클러스터링 파라미터에 의존합니다. 다른 resolution 값을 사용하면 결과가 달라질 수 있습니다.

4. **계산 비용**: 대규모 데이터셋에서 일부 지표(특히 pairwise 계산)는 계산량이 많을 수 있습니다. 필요시 샘플링을 고려하세요.

---

## 참고 문헌

1. Friedman, D., & Dieng, A. B. (2023). The Vendi Score: A Diversity Evaluation Metric for Machine Learning. ICLR.

2. Kynkäänniemi, T., et al. (2019). Improved Precision and Recall Metric for Assessing Generative Models. NeurIPS.

3. Naeem, M. F., et al. (2020). Reliable Fidelity and Diversity Metrics for Generative Models. ICML.

4. Xu, Y., et al. (2022). On the Diversity of Autonomous Driving Datasets. CVPR Workshop.

5. Yu, F., et al. (2020). BDD100K: A Diverse Driving Dataset for Heterogeneous Multitask Learning. CVPR.

6. Caesar, H., et al. (2020). nuScenes: A multimodal dataset for autonomous driving. CVPR.

---

## 버전 정보

- **버전**: 1.0.0
- **최종 업데이트**: 2025-01
- **호환성**: Python 3.9+, NumPy 1.24+, scikit-learn 1.3+
